specVersion: "v1beta"
serviceId: "healthy_age_backend"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "healthy_age_backend-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
